module.exports = function(grunt){

	// Configuration
	grunt.initConfig({
		// pass options to plugins, references to files etc
	    serve: {
	        options: {
	            port: 9000,
	            't': {
	            	outputUrl: 'index.html'
	            }
	        }
	    }
	});
}